[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  dotEnv = ".env"

[functions]
  node_bundler = "esbuild"

[[scheduled.functions]]
  path = "sync-snapshot"
  schedule = "0 0 */4 * *"
